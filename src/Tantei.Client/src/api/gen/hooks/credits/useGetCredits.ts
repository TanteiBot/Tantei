/**
 * Generated by Kubb (https://kubb.dev/).
 * Do not edit manually.
 */

import type {
  QueryKey,
  QueryClient,
  UseQueryOptions,
  UseQueryReturnType,
} from "@tanstack/vue-query";
import { queryOptions, useQuery } from "@tanstack/vue-query";
import { toValue } from "vue";

import type { RequestConfig, ResponseErrorConfig } from "../../.kubb/client";
import { getCredits } from "../../clients/credits/getCredits";
import type { GetCreditsStatus200 } from "../../types/credits/GetCredits";

export const getCreditsQueryKey = () => [{ url: "/api/credits" }] as const;

export type GetCreditsQueryKey = ReturnType<typeof getCreditsQueryKey>;

export function getCreditsQueryOptions(
  config: Partial<Omit<RequestConfig, "path" | "query" | "body" | "headers" | "url">> = {},
) {
  const queryKey = getCreditsQueryKey();
  return queryOptions<GetCreditsStatus200, ResponseErrorConfig<Error>, GetCreditsStatus200>({
    queryKey,
    queryFn: async ({ signal }) => {
      const { data } = await getCredits({
        ...config,
        signal: config.signal ?? signal,
        throwOnError: true,
      });
      return data;
    },
  });
}

/**
 * {@link /api/credits}
 */
export function useGetCredits<
  TData = GetCreditsStatus200,
  TQueryData = GetCreditsStatus200,
  TQueryKey extends QueryKey = GetCreditsQueryKey,
>(
  options: {
    query?: Partial<
      UseQueryOptions<GetCreditsStatus200, ResponseErrorConfig<Error>, TData, TQueryData, TQueryKey>
    > & { client?: QueryClient };
    client?: Partial<Omit<RequestConfig, "path" | "query" | "body" | "headers" | "url">>;
  } = {},
) {
  const { query: queryConfig = {}, client: config = {} } = options ?? {};
  const { client: queryClient, ...resolvedOptions } = queryConfig;
  const queryKey =
    (resolvedOptions && "queryKey" in resolvedOptions
      ? toValue(resolvedOptions.queryKey)
      : undefined) ?? getCreditsQueryKey();

  const queryResult = useQuery(
    {
      ...getCreditsQueryOptions(config),
      ...resolvedOptions,
      queryKey,
    } as unknown as UseQueryOptions<
      GetCreditsStatus200,
      ResponseErrorConfig<Error>,
      TData,
      GetCreditsStatus200,
      TQueryKey
    >,
    toValue(queryClient),
  ) as UseQueryReturnType<TData, ResponseErrorConfig<Error>> & { queryKey: TQueryKey };

  queryResult.queryKey = queryKey as TQueryKey;

  return queryResult;
}

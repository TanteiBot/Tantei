/**
 * Generated by Kubb (https://kubb.dev/).
 * Do not edit manually.
 */

import type {
  QueryKey,
  QueryClient,
  UseQueryOptions,
  UseQueryReturnType,
} from "@tanstack/vue-query";
import type { RequestConfig, ResponseErrorConfig } from "../../.kubb/client";
import type { GetCurrentUserStatus200 } from "../../types/auth/GetCurrentUser";
import { queryOptions, useQuery } from "@tanstack/vue-query";
import { getCurrentUser } from "../../clients/auth/getCurrentUser";
import { toValue } from "vue";

export const getCurrentUserQueryKey = () => [{ url: "/api/auth/me" }] as const;

export type GetCurrentUserQueryKey = ReturnType<typeof getCurrentUserQueryKey>;

export function getCurrentUserQueryOptions(
  config: Partial<Omit<RequestConfig, "path" | "query" | "body" | "headers" | "url">> = {},
) {
  const queryKey = getCurrentUserQueryKey();
  return queryOptions<GetCurrentUserStatus200, ResponseErrorConfig<Error>, GetCurrentUserStatus200>(
    {
      queryKey,
      queryFn: async ({ signal }) => {
        const { data } = await getCurrentUser({
          ...config,
          signal: config.signal ?? signal,
          throwOnError: true,
        });
        return data;
      },
    },
  );
}

/**
 * {@link /api/auth/me}
 */
export function useGetCurrentUser<
  TData = GetCurrentUserStatus200,
  TQueryData = GetCurrentUserStatus200,
  TQueryKey extends QueryKey = GetCurrentUserQueryKey,
>(
  options: {
    query?: Partial<
      UseQueryOptions<
        GetCurrentUserStatus200,
        ResponseErrorConfig<Error>,
        TData,
        TQueryData,
        TQueryKey
      >
    > & { client?: QueryClient };
    client?: Partial<Omit<RequestConfig, "path" | "query" | "body" | "headers" | "url">>;
  } = {},
) {
  const { query: queryConfig = {}, client: config = {} } = options ?? {};
  const { client: queryClient, ...resolvedOptions } = queryConfig;
  const queryKey =
    (resolvedOptions && "queryKey" in resolvedOptions
      ? toValue(resolvedOptions.queryKey)
      : undefined) ?? getCurrentUserQueryKey();

  const queryResult = useQuery(
    {
      ...getCurrentUserQueryOptions(config),
      ...resolvedOptions,
      queryKey,
    } as unknown as UseQueryOptions<
      GetCurrentUserStatus200,
      ResponseErrorConfig<Error>,
      TData,
      GetCurrentUserStatus200,
      TQueryKey
    >,
    toValue(queryClient),
  ) as UseQueryReturnType<TData, ResponseErrorConfig<Error>> & { queryKey: TQueryKey };

  queryResult.queryKey = queryKey as TQueryKey;

  return queryResult;
}

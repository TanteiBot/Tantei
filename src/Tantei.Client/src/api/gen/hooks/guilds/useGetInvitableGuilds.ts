/**
 * Generated by Kubb (https://kubb.dev/).
 * Do not edit manually.
 */

import type {
  QueryKey,
  QueryClient,
  UseQueryOptions,
  UseQueryReturnType,
} from "@tanstack/vue-query";
import type { RequestConfig, ResponseErrorConfig } from "../../.kubb/client";
import type {
  GetInvitableGuildsStatus200,
  GetInvitableGuildsStatus401,
} from "../../types/guilds/GetInvitableGuilds";
import { queryOptions, useQuery } from "@tanstack/vue-query";
import { getInvitableGuilds } from "../../clients/guilds/getInvitableGuilds";
import { toValue } from "vue";

export const getInvitableGuildsQueryKey = () => [{ url: "/api/guilds/invitable" }] as const;

export type GetInvitableGuildsQueryKey = ReturnType<typeof getInvitableGuildsQueryKey>;

export function getInvitableGuildsQueryOptions(
  config: Partial<Omit<RequestConfig, "path" | "query" | "body" | "headers" | "url">> = {},
) {
  const queryKey = getInvitableGuildsQueryKey();
  return queryOptions<
    GetInvitableGuildsStatus200,
    ResponseErrorConfig<GetInvitableGuildsStatus401>,
    GetInvitableGuildsStatus200
  >({
    queryKey,
    queryFn: async ({ signal }) => {
      const { data } = await getInvitableGuilds({
        ...config,
        signal: config.signal ?? signal,
        throwOnError: true,
      });
      return data;
    },
  });
}

/**
 * {@link /api/guilds/invitable}
 */
export function useGetInvitableGuilds<
  TData = GetInvitableGuildsStatus200,
  TQueryData = GetInvitableGuildsStatus200,
  TQueryKey extends QueryKey = GetInvitableGuildsQueryKey,
>(
  options: {
    query?: Partial<
      UseQueryOptions<
        GetInvitableGuildsStatus200,
        ResponseErrorConfig<GetInvitableGuildsStatus401>,
        TData,
        TQueryData,
        TQueryKey
      >
    > & { client?: QueryClient };
    client?: Partial<Omit<RequestConfig, "path" | "query" | "body" | "headers" | "url">>;
  } = {},
) {
  const { query: queryConfig = {}, client: config = {} } = options ?? {};
  const { client: queryClient, ...resolvedOptions } = queryConfig;
  const queryKey =
    (resolvedOptions && "queryKey" in resolvedOptions
      ? toValue(resolvedOptions.queryKey)
      : undefined) ?? getInvitableGuildsQueryKey();

  const queryResult = useQuery(
    {
      ...getInvitableGuildsQueryOptions(config),
      ...resolvedOptions,
      queryKey,
    } as unknown as UseQueryOptions<
      GetInvitableGuildsStatus200,
      ResponseErrorConfig<GetInvitableGuildsStatus401>,
      TData,
      GetInvitableGuildsStatus200,
      TQueryKey
    >,
    toValue(queryClient),
  ) as UseQueryReturnType<TData, ResponseErrorConfig<GetInvitableGuildsStatus401>> & {
    queryKey: TQueryKey;
  };

  queryResult.queryKey = queryKey as TQueryKey;

  return queryResult;
}

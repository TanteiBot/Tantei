/**
 * Generated by Kubb (https://kubb.dev/).
 * Do not edit manually.
 */

import type {
  QueryKey,
  QueryClient,
  UseQueryOptions,
  UseQueryReturnType,
} from "@tanstack/vue-query";
import { queryOptions, useQuery } from "@tanstack/vue-query";
import { toValue } from "vue";

import type { RequestConfig, ResponseErrorConfig } from "../../.kubb/client";
import { ping } from "../../clients/status/ping";
import type { PingStatus200, PingStatus401, PingStatus403 } from "../../types/status/Ping";

export const pingQueryKey = () => [{ url: "/api/ping" }] as const;

export type PingQueryKey = ReturnType<typeof pingQueryKey>;

export function pingQueryOptions(
  config: Partial<Omit<RequestConfig, "path" | "query" | "body" | "headers" | "url">> = {},
) {
  const queryKey = pingQueryKey();
  return queryOptions<
    PingStatus200,
    ResponseErrorConfig<PingStatus401 | PingStatus403>,
    PingStatus200
  >({
    queryKey,
    queryFn: async ({ signal }) => {
      const { data } = await ping({
        ...config,
        signal: config.signal ?? signal,
        throwOnError: true,
      });
      return data;
    },
  });
}

/**
 * {@link /api/ping}
 */
export function usePing<
  TData = PingStatus200,
  TQueryData = PingStatus200,
  TQueryKey extends QueryKey = PingQueryKey,
>(
  options: {
    query?: Partial<
      UseQueryOptions<
        PingStatus200,
        ResponseErrorConfig<PingStatus401 | PingStatus403>,
        TData,
        TQueryData,
        TQueryKey
      >
    > & { client?: QueryClient };
    client?: Partial<Omit<RequestConfig, "path" | "query" | "body" | "headers" | "url">>;
  } = {},
) {
  const { query: queryConfig = {}, client: config = {} } = options ?? {};
  const { client: queryClient, ...resolvedOptions } = queryConfig;
  const queryKey =
    (resolvedOptions && "queryKey" in resolvedOptions
      ? toValue(resolvedOptions.queryKey)
      : undefined) ?? pingQueryKey();

  const queryResult = useQuery(
    {
      ...pingQueryOptions(config),
      ...resolvedOptions,
      queryKey,
    } as unknown as UseQueryOptions<
      PingStatus200,
      ResponseErrorConfig<PingStatus401 | PingStatus403>,
      TData,
      PingStatus200,
      TQueryKey
    >,
    toValue(queryClient),
  ) as UseQueryReturnType<TData, ResponseErrorConfig<PingStatus401 | PingStatus403>> & {
    queryKey: TQueryKey;
  };

  queryResult.queryKey = queryKey as TQueryKey;

  return queryResult;
}

/**
 * Generated by Kubb (https://kubb.dev/).
 * Do not edit manually.
 */

import type {
  QueryKey,
  QueryClient,
  UseQueryOptions,
  UseQueryReturnType,
} from "@tanstack/vue-query";
import { queryOptions, useQuery } from "@tanstack/vue-query";
import { toValue } from "vue";

import type { RequestConfig, ResponseErrorConfig } from "../../.kubb/client";
import { getSiteConfig } from "../../clients/config/getSiteConfig";
import type { GetSiteConfigStatus200 } from "../../types/config/GetSiteConfig";

export const getSiteConfigQueryKey = () => [{ url: "/api/config" }] as const;

export type GetSiteConfigQueryKey = ReturnType<typeof getSiteConfigQueryKey>;

export function getSiteConfigQueryOptions(
  config: Partial<Omit<RequestConfig, "path" | "query" | "body" | "headers" | "url">> = {},
) {
  const queryKey = getSiteConfigQueryKey();
  return queryOptions<GetSiteConfigStatus200, ResponseErrorConfig<Error>, GetSiteConfigStatus200>({
    queryKey,
    queryFn: async ({ signal }) => {
      const { data } = await getSiteConfig({
        ...config,
        signal: config.signal ?? signal,
        throwOnError: true,
      });
      return data;
    },
  });
}

/**
 * {@link /api/config}
 */
export function useGetSiteConfig<
  TData = GetSiteConfigStatus200,
  TQueryData = GetSiteConfigStatus200,
  TQueryKey extends QueryKey = GetSiteConfigQueryKey,
>(
  options: {
    query?: Partial<
      UseQueryOptions<
        GetSiteConfigStatus200,
        ResponseErrorConfig<Error>,
        TData,
        TQueryData,
        TQueryKey
      >
    > & { client?: QueryClient };
    client?: Partial<Omit<RequestConfig, "path" | "query" | "body" | "headers" | "url">>;
  } = {},
) {
  const { query: queryConfig = {}, client: config = {} } = options ?? {};
  const { client: queryClient, ...resolvedOptions } = queryConfig;
  const queryKey =
    (resolvedOptions && "queryKey" in resolvedOptions
      ? toValue(resolvedOptions.queryKey)
      : undefined) ?? getSiteConfigQueryKey();

  const queryResult = useQuery(
    {
      ...getSiteConfigQueryOptions(config),
      ...resolvedOptions,
      queryKey,
    } as unknown as UseQueryOptions<
      GetSiteConfigStatus200,
      ResponseErrorConfig<Error>,
      TData,
      GetSiteConfigStatus200,
      TQueryKey
    >,
    toValue(queryClient),
  ) as UseQueryReturnType<TData, ResponseErrorConfig<Error>> & { queryKey: TQueryKey };

  queryResult.queryKey = queryKey as TQueryKey;

  return queryResult;
}

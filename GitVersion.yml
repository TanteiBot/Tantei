commit-date-format: o
update-build-number: false

branches:
  'all':
    regex: "release\/v2"
    label: all
    increment: None
    prevent-increment:
      when-current-commit-tagged: true
    pre-release-weight: 0